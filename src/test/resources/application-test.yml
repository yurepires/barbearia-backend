spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  sql:
    init:
      mode: never
  sendgrid:
    api-key: SG.fake-api-key-for-testing

barbearia:
  secret: chave-secreta-de-teste-com-pelo-menos-32-caracteres-para-hs256
